---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: web ページを使用したグラフでのデータASP.NET表示 (Razor) |マイクロソフトドキュメント
author: rick-anderson
description: この章では、グラフにデータを表示する方法について説明します。 前の章では、データを手動でグリッドに表示する方法を学習しました。 この章では説明します。
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: ad55d4898ee39e0239ef8b0bd5eea6387af3c816
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542886"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="fcea9-105">ASP.NET Web ページを使用したグラフでのデータの表示 (Razor)</span><span class="sxs-lookup"><span data-stu-id="fcea9-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="fcea9-106">[マイクロソフト](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="fcea9-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="fcea9-107">この記事では、ヘルパーを使用して、グラフを使用して ASP.NET Web ページ (Razor) `Chart` Web サイトでデータを表示する方法について説明します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="fcea9-108">**あなたが学ぶこと:**</span><span class="sxs-lookup"><span data-stu-id="fcea9-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="fcea9-109">グラフにデータを表示する方法。</span><span class="sxs-lookup"><span data-stu-id="fcea9-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="fcea9-110">組み込みのテーマを使用してグラフのスタイルを設定する方法。</span><span class="sxs-lookup"><span data-stu-id="fcea9-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="fcea9-111">グラフを保存する方法と、パフォーマンスを向上させるためにグラフをキャッシュする方法。</span><span class="sxs-lookup"><span data-stu-id="fcea9-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="fcea9-112">この記事で紹介されているASP.NETプログラミング機能は次のとおりです。</span><span class="sxs-lookup"><span data-stu-id="fcea9-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="fcea9-113">ヘルパー `Chart` 。</span><span class="sxs-lookup"><span data-stu-id="fcea9-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="fcea9-114">この資料の情報は、Web ページ 1.0 および Web ページ 2 ASP.NETに適用されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="fcea9-115">チャートヘルパー</span><span class="sxs-lookup"><span data-stu-id="fcea9-115">The Chart Helper</span></span>

<span data-ttu-id="fcea9-116">データをグラフィカルな形式で表示する場合は、ヘルパーを使用`Chart`できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="fcea9-117">ヘルパー`Chart`は、さまざまな種類のグラフでデータを表示するイメージをレンダリングできます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="fcea9-118">書式設定とラベル付けの多くのオプションをサポートしています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="fcea9-119">この`Chart`支援者は、Microsoft Excel や、面グラフ、棒グラフ、縦棒グラフ、折れ線グラフ、円グラフなどのツール&#8212;、30 種類以上のグラフを表示できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="fcea9-120">**面グラフ**![の説明 : 面グラフの種類の画像](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="fcea9-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="fcea9-121">**横棒グラフ**![の説明: 棒グラフの種類の画像](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="fcea9-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="fcea9-122">**縦棒グラフ**![の説明 : 縦棒グラフの種類の画像](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="fcea9-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="fcea9-123">**折れ線グラフ**![の説明: 折れ線グラフの種類の画像](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="fcea9-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="fcea9-124">**円グラフ**![の説明 : 円グラフの種類の画像](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="fcea9-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="fcea9-125">**株価チャート**![の説明: 株価チャートの種類の画像](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="fcea9-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="fcea9-126">グラフ要素</span><span class="sxs-lookup"><span data-stu-id="fcea9-126">Chart Elements</span></span>

<span data-ttu-id="fcea9-127">グラフには、凡例、軸、系列などのデータと追加要素が表示されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="fcea9-128">次の図は、`Chart`ヘルパーを使用するときにカスタマイズできるグラフ要素の多くを示しています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="fcea9-129">この記事では、これらの要素の一部 (すべてではない) を設定する方法について説明します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-129">This article shows you how to set some (not all) of these elements.</span></span>

![説明: グラフ要素を示す画像](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="fcea9-131">データからグラフを作成する</span><span class="sxs-lookup"><span data-stu-id="fcea9-131">Creating a Chart from Data</span></span>

<span data-ttu-id="fcea9-132">グラフに表示するデータは、配列、データベースから返された結果、または XML ファイル内のデータから取得できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="fcea9-133">配列の使用</span><span class="sxs-lookup"><span data-stu-id="fcea9-133">Using an Array</span></span>

<span data-ttu-id="fcea9-134">[「Razor 構文を使用した web ページプログラミング ASP.NETの概要](https://go.microsoft.com/fwlink/?LinkId=202890)」で説明したように、配列を使用すると、類似した項目のコレクションを 1 つの変数に格納できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="fcea9-135">配列を使用して、グラフに含めるデータを格納できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="fcea9-136">この手順では、既定のグラフの種類を使用して、配列内のデータからグラフを作成する方法を示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="fcea9-137">また、ページ内にグラフを表示する方法も示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="fcea9-138">という名前の新しいファイル*を作成します*。</span><span class="sxs-lookup"><span data-stu-id="fcea9-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="fcea9-139">既存のコンテンツを次の内容に置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="fcea9-140">コードは、まず新しいグラフを作成し、その幅と高さを設定します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="fcea9-141">この方法を使用して、グラフの`AddTitle`タイトルを指定します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="fcea9-142">データを追加するには、 メソッド`AddSeries`を使用します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="fcea9-143">この例では、メソッド`name`の 、 `xValue`、`yValues`および の`AddSeries`パラメーターを使用します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="fcea9-144">パラメーター`name`は、グラフの凡例に表示されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="fcea9-145">この`xValue`パラメーターには、グラフの横軸に沿って表示されるデータの配列が含まれます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="fcea9-146">この`yValues`パラメータには、グラフの垂直ポイントをプロットするために使用されるデータの配列が含まれています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="fcea9-147">この`Write`メソッドは、実際にグラフをレンダリングします。</span><span class="sxs-lookup"><span data-stu-id="fcea9-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="fcea9-148">この場合、グラフの種類を指定していないため、ヘルパーは既定の`Chart`グラフ (縦棒グラフ) をレンダリングします。</span><span class="sxs-lookup"><span data-stu-id="fcea9-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="fcea9-149">ブラウザでページを実行します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-149">Run the page in the browser.</span></span> <span data-ttu-id="fcea9-150">ブラウザにグラフが表示されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="fcea9-151">グラフ データに対するデータベース クエリの使用</span><span class="sxs-lookup"><span data-stu-id="fcea9-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="fcea9-152">グラフにする情報がデータベースにある場合は、データベース クエリを実行し、結果のデータを使用してグラフを作成できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="fcea9-153">この手順では[、「Web ページ サイト](https://go.microsoft.com/fwlink/?LinkId=202893)のデータベースの概要」の記事で作成したデータベースのデータを読み取って表示する方法ASP.NET示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="fcea9-154">フォルダーが存在しない場合は *、Web\_* サイトのルートにアプリ データ フォルダーを追加します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="fcea9-155">*[\_アプリ データ*] フォルダで、「Web ページ サイトでのデータベースの操作の概要 」で説明されている*SmallBakery.sdf*という[名前のデータベース](https://go.microsoft.com/fwlink/?LinkId=202893)ファイルASP.NET追加します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="fcea9-156">という名前の新しいファイル*を作成します*。</span><span class="sxs-lookup"><span data-stu-id="fcea9-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="fcea9-157">既存のコンテンツを次の内容に置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="fcea9-158">コードは最初に SmallBakery データベースを開き、 という名前`db`の変数に割り当てます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="fcea9-159">この変数は、`Database`データベースの読み取りおよび書き込みに使用できるオブジェクトを表します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="fcea9-160">次に、各製品の名前と価格を取得する SQL クエリを実行します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="fcea9-161">このコードは、新しいグラフを作成し、グラフの`DataBindTable`メソッドを呼び出してデータベース クエリをそれに渡します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="fcea9-162">このメソッドは、`dataSource`クエリのデータに対するパラメータと、グラフの X`xField`軸に使用するデータ列を設定するパラメータの 2 つのパラメーターを受け取ります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="fcea9-163">`DataBindTable`メソッドを使用する代わりに、ヘルパーのメソッドを`AddSeries``Chart`使用できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="fcea9-164">この`AddSeries`メソッドでは、 および`xValue``yValues`パラメーターを設定できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="fcea9-165">たとえば、次のような方法を使用`DataBindTable`する代わりに、</span><span class="sxs-lookup"><span data-stu-id="fcea9-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="fcea9-166">次のような方法を`AddSeries`使用できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="fcea9-167">どちらも同じ結果をレンダリングします。</span><span class="sxs-lookup"><span data-stu-id="fcea9-167">Both render the same results.</span></span> <span data-ttu-id="fcea9-168">この`AddSeries`方法は、グラフの種類とデータをより明確に指定できるため、より柔軟ですが、柔軟性`DataBindTable`が必要ない場合は、この方法を使用する方が簡単です。</span><span class="sxs-lookup"><span data-stu-id="fcea9-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="fcea9-169">ブラウザでページを実行します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="fcea9-170">XML データの使用</span><span class="sxs-lookup"><span data-stu-id="fcea9-170">Using XML Data</span></span>

<span data-ttu-id="fcea9-171">グラフ作成の 3 番目のオプションは、XML ファイルをグラフのデータとして使用することです。</span><span class="sxs-lookup"><span data-stu-id="fcea9-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="fcea9-172">これには、XML ファイルにも、XML 構造を記述するスキーマ ファイル (*.xsd*ファイル ) が含まれている必要があります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="fcea9-173">この手順では、XML ファイルからデータを読み取る方法を示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="fcea9-174">*[\_アプリケーション データ*] フォルダに *、data.xml*という名前の新しい XML ファイルを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="fcea9-175">既存の XML を次の XML (架空の会社の従業員に関する XML データ) に置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="fcea9-176">*[\_アプリケーション データ*] フォルダに *、data.xsd*という名前の新しい XML ファイルを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="fcea9-177">(今回の拡張子は *.xsd*であることに注意してください。</span><span class="sxs-lookup"><span data-stu-id="fcea9-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="fcea9-178">既存の XML を次のように置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="fcea9-179">Web サイトのルートで *、ChartDataXML.cshtml*という名前の新しいファイルを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="fcea9-180">既存のコンテンツを次の内容に置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="fcea9-181">このコードでは、まず`DataSet`オブジェクトを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="fcea9-182">このオブジェクトは、XML ファイルから読み取られるデータを管理し、スキーマ ファイルの情報に従って整理するために使用されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="fcea9-183">(コードの先頭にステートメント`using SystemData`が含まれていることに注意してください。</span><span class="sxs-lookup"><span data-stu-id="fcea9-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="fcea9-184">`DataSet`これは、オブジェクトを操作するために必要です。</span><span class="sxs-lookup"><span data-stu-id="fcea9-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="fcea9-185">詳細については、この記事[&quot;の&quot;「ステートメントと完全修飾名の使用](#SB_UsingStatements)」を参照してください)。</span><span class="sxs-lookup"><span data-stu-id="fcea9-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="fcea9-186">次に、データセットに基`DataView`づいてオブジェクトを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="fcea9-187">データビューは、チャートが&#8212;、読み取り、プロットにバインドできるオブジェクトを提供します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="fcea9-188">このグラフは、配列データのチャート作成`AddSeries`で先に説明したように、この`xValue``yValues`メソッドを使用してデータにバインド`DataView`されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="fcea9-189">この例では、特定のグラフの種類を指定する方法も示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="fcea9-190">メソッドにデータが`AddSeries`追加されると、`chartType`パラメーターも円グラフを表示するように設定されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="fcea9-191">ブラウザでページを実行します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="fcea9-192">"using" ステートメントと完全修飾名</span><span class="sxs-lookup"><span data-stu-id="fcea9-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="fcea9-193">Razor 構文を使用して Web ページをASP.NETする .NET Framework は、何千ものコンポーネント (クラス) で構成されています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="fcea9-194">これらのすべてのクラスを管理しやすくするために、それらはライブラリに似た*名前空間*に編成されています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="fcea9-195">たとえば、`System.Web`名前空間にはブラウザー/サーバー通信をサポートするクラスが含まれ`System.Xml`、名前空間には XML ファイルの作成と読み取りを行`System.Data`うために使用されるクラスが含まれ、名前空間にはデータを操作するためのクラスが含まれます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="fcea9-196">.NET Framework 内の特定のクラスにアクセスするには、クラス名だけでなく、クラスが存在する名前空間もコードで認識する必要があります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="fcea9-197">たとえば、`Chart`ヘルパーを使用するには、`System.Web.Helpers.Chart`名前空間 ( ) とクラス名 (`System.Web.Helpers``Chart`) を組み合わせたクラスをコードで検索する必要があります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="fcea9-198">これは、.NET Framework の広大な範囲内で、クラスの*完全修飾*名&#8212;完全で明確な場所と呼ばれます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="fcea9-199">コードでは、次のようになります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="fcea9-200">ただし、クラスやヘルパーを参照するたびに、これらの長い完全修飾名を使用する必要がある場合は、面倒な (エラーが発生しやすい) 。</span><span class="sxs-lookup"><span data-stu-id="fcea9-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="fcea9-201">したがって、クラス名を使いやすくするために、関心のある名前空間を*インポート*できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="fcea9-202">名前空間をインポートした場合は、完全修飾名 ( ) の代わりに`Chart`クラス名 ( )`System.Web.Helpers.Chart`のみを使用できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="fcea9-203">コードが実行され、クラス名を検出すると、インポートした名前空間だけを検索してそのクラスを見つけることができます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="fcea9-204">Web ページASP.NET Razor 構文を使用して Web ページを作成する場合、通常は、`WebPage`クラス、さまざまなヘルパーなど、毎回同じクラスのセットを使用します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="fcea9-205">Web サイトを作成するたびに関連する名前空間をインポートする作業を節約するために、ASP.NETは、各 Web サイトのコア名前空間のセットを自動的にインポートするように構成されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="fcea9-206">そのため、これまで名前空間を処理したり、インポートしたりする必要はありません。作業したすべてのクラスは、既にインポートされている名前空間に含まれています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="fcea9-207">ただし、自動的にインポートされる名前空間にないクラスを使用する必要がある場合があります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="fcea9-208">その場合は、そのクラスの完全修飾名を使用するか、クラスを含む名前空間を手動でインポートできます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="fcea9-209">名前空間をインポートするには、前の例`using`で説明`import`したように、Visual Basic でステートメントを使用します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="fcea9-210">たとえば、`DataSet`クラスは名前空間にあります`System.Data`。</span><span class="sxs-lookup"><span data-stu-id="fcea9-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="fcea9-211">名前空間`System.Data`は、Razor ページASP.NET自動的には使用できません。</span><span class="sxs-lookup"><span data-stu-id="fcea9-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="fcea9-212">したがって、完全修飾名を`DataSet`使用してクラスを操作するには、次のようなコードを使用できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="fcea9-213">`DataSet`クラスを繰り返し使用する必要がある場合は、次のような名前空間をインポートし、コードでクラス名だけを使用できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="fcea9-214">参照する他`using`の .NET Framework 名前空間のステートメントを追加できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="fcea9-215">ただし、前述のように、ほとんどのクラスは *、.cshtml*および *.vbhtml*ページで使用するためにASP.NETによって自動的にインポートされる名前空間に含まれているため、頻繁に行う必要はありません。</span><span class="sxs-lookup"><span data-stu-id="fcea9-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="fcea9-216">Web ページ内にグラフを表示する</span><span class="sxs-lookup"><span data-stu-id="fcea9-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="fcea9-217">これまでに説明した例では、グラフを作成し、グラフがブラウザーに直接グラフィックスとして表示されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="fcea9-218">ただし、多くの場合、グラフは、ブラウザー内で単独で表示されるのではなく、ページの一部として表示する必要があります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="fcea9-219">そのためには、2段階のプロセスが必要です。</span><span class="sxs-lookup"><span data-stu-id="fcea9-219">To do that requires a two-step process.</span></span> <span data-ttu-id="fcea9-220">最初の手順は、既に見てきたように、グラフを生成するページを作成することです。</span><span class="sxs-lookup"><span data-stu-id="fcea9-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="fcea9-221">2 番目の手順では、別のページに結果の画像を表示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="fcea9-222">画像を表示するには、任意の画像を`<img>`表示するのと同じ方法で HTML 要素を使用します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="fcea9-223">ただし *、.jpg*ファイルまたは *.png*ファイルを参照する`<img>`代わりに、要素は、グラフを作成する`Chart`ヘルパーを含む *.cshtml*ファイルを参照します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="fcea9-224">表示ページが実行されると、要素`<img>`はヘルパーの出力を`Chart`取得し、グラフをレンダリングします。</span><span class="sxs-lookup"><span data-stu-id="fcea9-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="fcea9-225">という名前のファイル*を作成します*。</span><span class="sxs-lookup"><span data-stu-id="fcea9-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="fcea9-226">既存のコンテンツを次の内容に置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="fcea9-227">コードでは、要素`<img>`を使用して *、ChartArrayBasic.cshtml*ファイルで作成したグラフを表示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="fcea9-228">ブラウザーで Web ページを実行します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-228">Run the web page in a browser.</span></span> <span data-ttu-id="fcea9-229">ファイルには *、ChartArrayBasic.cshtml*ファイルに含まれるコードに基づいてグラフ イメージが表示されます。 *ShowChart.cshtml*</span><span class="sxs-lookup"><span data-stu-id="fcea9-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="fcea9-230">グラフのスタイル設定</span><span class="sxs-lookup"><span data-stu-id="fcea9-230">Styling a Chart</span></span>

<span data-ttu-id="fcea9-231">ヘルパー`Chart`は、グラフの外観をカスタマイズできる多数のオプションをサポートしています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="fcea9-232">色、フォント、罫線などを設定できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="fcea9-233">グラフの外観をカスタマイズする簡単な方法は、 テーマ を使用*することです*。</span><span class="sxs-lookup"><span data-stu-id="fcea9-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="fcea9-234">テーマは、フォント、色、ラベル、パレット、枠線、効果を使用したグラフの表示方法を指定する情報の集まりです。</span><span class="sxs-lookup"><span data-stu-id="fcea9-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="fcea9-235">(グラフのスタイルはグラフの種類を示しません)。</span><span class="sxs-lookup"><span data-stu-id="fcea9-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="fcea9-236">次の表は、組み込みのテーマの一覧です。</span><span class="sxs-lookup"><span data-stu-id="fcea9-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="fcea9-237">テーマ</span><span class="sxs-lookup"><span data-stu-id="fcea9-237">Theme</span></span> | <span data-ttu-id="fcea9-238">説明</span><span class="sxs-lookup"><span data-stu-id="fcea9-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="fcea9-239">白い背景に赤い列を表示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="fcea9-240">青いグラデーションの背景に青い列を表示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="fcea9-241">緑のグラデーションの背景に青い列を表示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="fcea9-242">黄色のグラデーションの背景にオレンジ色の列を表示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="fcea9-243">白い背景に 3-D の赤い列を表示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="fcea9-244">新しいグラフを作成するときに使用するテーマを指定できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="fcea9-245">という名前の新しいファイル*を作成します*。</span><span class="sxs-lookup"><span data-stu-id="fcea9-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="fcea9-246">ページ内の既存のコンテンツを次の内容に置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="fcea9-247">このコードは、データベースをデータに使用する前の例と同じですが、オブジェクト`theme`を作成するときにパラメータを`Chart`追加します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="fcea9-248">変更されたコードを次に示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="fcea9-249">ブラウザでページを実行します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-249">Run the page in a browser.</span></span> <span data-ttu-id="fcea9-250">以前と同じデータが表示されますが、グラフはより洗練された外観になります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="fcea9-251">グラフの保存</span><span class="sxs-lookup"><span data-stu-id="fcea9-251">Saving a Chart</span></span>

<span data-ttu-id="fcea9-252">この記事で説明`Chart`したようにヘルパーを使用すると、ヘルパーは呼び出されるたびにグラフを最初から再作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="fcea9-253">必要に応じて、グラフのコードもデータベースを再クエリするか、XML ファイルを再読み込みしてデータを取得します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="fcea9-254">クエリを実行しているデータベースが大きい場合や、XML ファイルに大量のデータが含まれている場合など、複雑な操作が行われる場合があります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="fcea9-255">グラフに大量のデータが含まれていない場合でも、画像を動的に作成するプロセスはサーバーリソースを占有し、多くのユーザーがグラフを表示するページを要求すると、Web サイトのパフォーマンスに影響を与える可能性があります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="fcea9-256">グラフを作成した場合のパフォーマンスへの影響を軽減するために、グラフを初めて作成して保存します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="fcea9-257">グラフが再生成されるのではなく、再び必要になったときに、保存したバージョンを取得してレンダリングするだけで済みます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="fcea9-258">グラフは次の方法で保存できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="fcea9-259">(サーバー上の) コンピュータメモリにグラフをキャッシュします。</span><span class="sxs-lookup"><span data-stu-id="fcea9-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="fcea9-260">グラフを画像ファイルとして保存します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="fcea9-261">グラフを XML ファイルとして保存します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-261">Save the chart as an XML file.</span></span> <span data-ttu-id="fcea9-262">このオプションを使用すると、保存する前にグラフを変更できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="fcea9-263">チャートをキャッシュする</span><span class="sxs-lookup"><span data-stu-id="fcea9-263">Caching a Chart</span></span>

<span data-ttu-id="fcea9-264">グラフを作成したら、それをキャッシュできます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="fcea9-265">グラフをキャッシュすると、グラフを再度表示する必要がある場合に再作成する必要はありません。</span><span class="sxs-lookup"><span data-stu-id="fcea9-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="fcea9-266">キャッシュにグラフを保存する場合は、そのグラフに固有のキーを指定します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="fcea9-267">サーバーのメモリが不足している場合、キャッシュに保存されたグラフが削除されることがあります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="fcea9-268">また、何らかの理由でアプリケーションが再起動されると、キャッシュはクリアされます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="fcea9-269">したがって、キャッシュされたグラフを使用する標準的な方法は、キャッシュで使用できるかどうかを常に確認し、キャッシュ内で使用可能でない場合は作成または再作成することです。</span><span class="sxs-lookup"><span data-stu-id="fcea9-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="fcea9-270">Web サイトのルートで *、ShowCachedChart.cshtml*という名前のファイルを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="fcea9-271">既存のコンテンツを次の内容に置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="fcea9-272">この`<img>`タグには`src`*、ChartSaveToCache.cshtml*ファイルを指し示す属性が含まれており、キーをクエリ文字列としてページに渡します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="fcea9-273">キーには、myChartKey&quot;&quot;という値が含まれています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="fcea9-274">グラフを作成*する*ヘルパーが`Chart`含まれています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="fcea9-275">このページは、すぐに作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="fcea9-276">ページの最後に *、ClearCache.cshtml*という名前のページへのリンクがあります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="fcea9-277">これは、すぐに作成するページです。</span><span class="sxs-lookup"><span data-stu-id="fcea9-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="fcea9-278">この例のキャッシュをテストする場合にのみ *、ClearCache.cshtml*が必要です。</span><span class="sxs-lookup"><span data-stu-id="fcea9-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="fcea9-279">Web サイトのルートで *、ChartSaveToCache.cshtml*という名前の新しいファイルを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="fcea9-280">既存のコンテンツを次の内容に置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="fcea9-281">コードは、クエリ文字列のキー値として渡されたものがあるかどうかを最初にチェックします。</span><span class="sxs-lookup"><span data-stu-id="fcea9-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="fcea9-282">その場合、コードはメソッドを呼び出してキーを渡すことによって、キャッシュ`GetFromCache`からグラフを読み取ろうとします。</span><span class="sxs-lookup"><span data-stu-id="fcea9-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="fcea9-283">キャッシュ内にキーの下に何も存在しないことがわかった場合 (これは、グラフが最初に要求された時に発生します)、コードは通常どおりグラフを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="fcea9-284">グラフが終了すると、コードは を呼び出`SaveToCache`してキャッシュに保存します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="fcea9-285">このメソッドにはキー (チャートを後で要求できるように) と、グラフをキャッシュに保存する時間が必要です。</span><span class="sxs-lookup"><span data-stu-id="fcea9-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="fcea9-286">(グラフをキャッシュする正確な時間は、グラフが表すデータが変更される可能性があると考える頻度によって異なります)。この`SaveToCache`メソッドでは`slidingExpiration`、パラメーター &#8212;が true に設定されている場合、グラフにアクセスするたびにタイムアウト カウンタがリセットされます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="fcea9-287">この場合、チャートのキャッシュエントリは、最後に誰かがチャートにアクセスした 2 分後に期限切れになります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="fcea9-288">(有効期限のスライドの代わりに絶対有効期限が設定されている場合は、キャッシュ エントリがキャッシュに格納されてから 2 分後に、アクセス頻度にかかわらず、キャッシュ エントリの有効期限が切れます)。</span><span class="sxs-lookup"><span data-stu-id="fcea9-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="fcea9-289">最後に、このメソッドを`WriteFromCache`使用して、キャッシュからチャートをフェッチしてレンダリングします。</span><span class="sxs-lookup"><span data-stu-id="fcea9-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="fcea9-290">このメソッドは、キャッシュを`if`チェックするブロックの外側にあるので、グラフが最初に存在するか、生成してキャッシュに保存する必要があったかにかかわらず、キャッシュからグラフを取得します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="fcea9-291">この例では、メソッドに`AddTitle`タイムスタンプが含まれていることに注意してください。</span><span class="sxs-lookup"><span data-stu-id="fcea9-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="fcea9-292">(タイトルに現在の日付と時刻&#8212;&#8212;`DateTime.Now`が追加されます)。</span><span class="sxs-lookup"><span data-stu-id="fcea9-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="fcea9-293">*ClearCache.cshtml*という名前の新しいページを作成し、その内容を次のように置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="fcea9-294">このページでは、`WebCache`ヘルパーを使用して*ChartSaveToCache.cshtml*にキャッシュされているグラフを削除します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="fcea9-295">前述のように、通常このようなページを持つ必要はありません。</span><span class="sxs-lookup"><span data-stu-id="fcea9-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="fcea9-296">ここでは、キャッシュのテストを簡単にするために作成しています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="fcea9-297">ブラウザーで Web ページを*実行*します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="fcea9-298">このページには *、ChartSaveToCache.cshtml*ファイルに含まれるコードに基づいてグラフ イメージが表示されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="fcea9-299">タイムスタンプがチャートタイトルに何を示しているのかを書き留めます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![説明:チャートタイトルのタイムスタンプ付きの基本的なチャートの画像](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="fcea9-301">ブラウザーを閉じます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-301">Close the browser.</span></span>
8. <span data-ttu-id="fcea9-302">もう一度*実行します*。</span><span class="sxs-lookup"><span data-stu-id="fcea9-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="fcea9-303">タイムスタンプは前と同じで、チャートは再生成されず、キャッシュから読み取られたことを示します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="fcea9-304">[\*キャッシュの\***クリア**] リンクをクリックします。</span><span class="sxs-lookup"><span data-stu-id="fcea9-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="fcea9-305">これにより、キャッシュがクリアされたことを報告する*ClearCache.cshtml*が表示されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="fcea9-306">リンク**をクリック**するか、WebMatrix から*ShowCachedChart.cshtml を*再実行します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="fcea9-307">キャッシュがクリアされたため、今回はタイムスタンプが変更されています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="fcea9-308">そのため、コードはグラフを再生成し、キャッシュに戻す必要がありました。</span><span class="sxs-lookup"><span data-stu-id="fcea9-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="fcea9-309">グラフをイメージ ファイルとして保存する</span><span class="sxs-lookup"><span data-stu-id="fcea9-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="fcea9-310">また、グラフをイメージ ファイルとして保存することもできます (たとえば *、.jpg*ファイルとして) サーバー上でします。</span><span class="sxs-lookup"><span data-stu-id="fcea9-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="fcea9-311">その後、イメージファイルを任意のイメージと同じように使用できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="fcea9-312">この利点は、一時キャッシュに保存するのではなく、ファイルが保存される点です。</span><span class="sxs-lookup"><span data-stu-id="fcea9-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="fcea9-313">新しいグラフ イメージを異なる時間 (たとえば、1 時間ごと) に保存し、時間の経過に伴う変更を永続的に記録しておくことができます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="fcea9-314">Web アプリケーションに、イメージ ファイルを配置するサーバー上のフォルダーにファイルを保存する権限があることを確認する必要があります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="fcea9-315">Web サイトのルートに*\_ChartFiles*という名前のフォルダーが存在しない場合は、フォルダーを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="fcea9-316">Web サイトのルートで *、ChartSave.cshtml*という名前の新しいファイルを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="fcea9-317">既存のコンテンツを次の内容に置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="fcea9-318">コードは、まず、メソッドを呼び出すことによって *.jpg*ファイル`File.Exists`が存在するかどうかを確認します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="fcea9-319">ファイルが存在しない場合、コードは配列から new`Chart`を作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="fcea9-320">今回は、コードがメソッドを`Save`呼び出し、`path`パラメーターを渡して、グラフを保存する場所のファイル パスとファイル名を指定します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="fcea9-321">ページの本文では、`<img>`要素はパスを使用して表示する *.jpg*ファイルを指します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="fcea9-322">*ファイル*を実行します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="fcea9-323">WebMatrix に戻ります。</span><span class="sxs-lookup"><span data-stu-id="fcea9-323">Return to WebMatrix.</span></span> <span data-ttu-id="fcea9-324">*chart01.jpg*という名前のイメージ ファイルが*\_ChartFiles*フォルダに保存されていることに注意してください。</span><span class="sxs-lookup"><span data-stu-id="fcea9-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="fcea9-325">グラフを XML ファイルとして保存する</span><span class="sxs-lookup"><span data-stu-id="fcea9-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="fcea9-326">最後に、グラフを XML ファイルとしてサーバーに保存できます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="fcea9-327">グラフをキャッシュしたり、グラフをファイルに保存したりする方法よりもこの方法を使用する利点は、必要に応じて、グラフを表示する前に XML を変更できることです。</span><span class="sxs-lookup"><span data-stu-id="fcea9-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="fcea9-328">アプリケーションには、イメージ ファイルを配置するサーバー上のフォルダーに対する読み取り/書き込みアクセス許可が必要です。</span><span class="sxs-lookup"><span data-stu-id="fcea9-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="fcea9-329">Web サイトのルートで *、ChartSaveXml.cshtml*という名前の新しいファイルを作成します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="fcea9-330">既存のコンテンツを次の内容に置き換えます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="fcea9-331">このコードは、XML ファイルを使用する点を除いて、以前に表示されたコードと似ています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="fcea9-332">このコードは、まず、メソッドを呼び出して XML`File.Exists`ファイルが存在するかどうかを確認します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="fcea9-333">ファイルが存在する場合、コードは新`Chart`しいオブジェクトを作成し、ファイル名を`themePath`パラメーターとして渡します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="fcea9-334">これにより、XML ファイル内のグラフに基づいてグラフが作成されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="fcea9-335">XML ファイルが存在しない場合、コードは標準グラフを作成し、それを保存する呼`SaveXml`び出しを行います。</span><span class="sxs-lookup"><span data-stu-id="fcea9-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="fcea9-336">前に見たように、`Write`グラフはメソッドを使用してレンダリングされます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="fcea9-337">キャッシュを表示したページと同様に、このコードにはグラフ タイトルにタイムスタンプが含まれています。</span><span class="sxs-lookup"><span data-stu-id="fcea9-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="fcea9-338">*ChartDisplayXMLChart.cshtml*という名前の新しいページを作成し、それに次のマークアップを追加します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="fcea9-339">ページを*実行*します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="fcea9-340">グラフが表示されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-340">The chart is displayed.</span></span> <span data-ttu-id="fcea9-341">チャートのタイトルのタイムスタンプを書き留めます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="fcea9-342">ブラウザーを閉じます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-342">Close the browser.</span></span>
6. <span data-ttu-id="fcea9-343">WebMatrix で*\_、ChartFiles*フォルダーを右クリックし、 (**更新)** をクリックして、フォルダーを開きます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="fcea9-344">このフォルダー内の*XMLChart.xml*ファイルは`Chart`ヘルパーによって作成されました。</span><span class="sxs-lookup"><span data-stu-id="fcea9-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![説明: グラフ ヘルパーによって作成された XMLChart.xml ファイルを示す_ChartFiles フォルダー。](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="fcea9-346">もう一度 *[グラフ表示XML チャート.cshtml]* ページを実行します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="fcea9-347">チャートには、ページを初めて実行した時と同じタイムスタンプが表示されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="fcea9-348">これは、先ほど保存した XML からグラフが生成されるためです。</span><span class="sxs-lookup"><span data-stu-id="fcea9-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="fcea9-349">マトリックスで、*\_グラフ ファイル*フォルダーを開き *、XMLChart.xml*ファイルを削除します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="fcea9-350">もう一度*ページを実行します*。</span><span class="sxs-lookup"><span data-stu-id="fcea9-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="fcea9-351">今回は、ヘルパーが XML ファイルを`Chart`再作成する必要があるため、タイムスタンプが更新されます。</span><span class="sxs-lookup"><span data-stu-id="fcea9-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="fcea9-352">必要に応じ*\_、ChartFiles*フォルダを確認し、XML ファイルが戻っていることを確認します。</span><span class="sxs-lookup"><span data-stu-id="fcea9-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="fcea9-353">その他のリソース</span><span class="sxs-lookup"><span data-stu-id="fcea9-353">Additional Resources</span></span>

- [<span data-ttu-id="fcea9-354">ASP.NET Web ページ サイトでのデータベースの操作の概要</span><span class="sxs-lookup"><span data-stu-id="fcea9-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="fcea9-355">ASP.NET Web ページ サイトでキャッシュを使用してパフォーマンスを向上させる</span><span class="sxs-lookup"><span data-stu-id="fcea9-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="fcea9-356">[グラフ クラス](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99))(msdn ASP.NET Web ページ API リファレンス)</span><span class="sxs-lookup"><span data-stu-id="fcea9-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
