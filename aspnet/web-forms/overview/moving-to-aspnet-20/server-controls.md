---
uid: web-forms/overview/moving-to-aspnet-20/server-controls
title: サーバー コントロール |マイクロソフトドキュメント
author: rick-anderson
description: ASP.NET 2.0 では、サーバー コントロールがさまざまな点で強化されています。 このモジュールでは、2.0 と Visual Studio 200.. の方法ASP.NETアーキテクチャの変更点をいくつか取り上げます。
ms.author: riande
ms.date: 02/20/2005
ms.assetid: 43f6ac47-76fc-4cf7-8e9f-c18ce673dfd8
msc.legacyurl: /web-forms/overview/moving-to-aspnet-20/server-controls
msc.type: authoredcontent
ms.openlocfilehash: 7109f10e87abfadf1e7e08795cf9d3d6bf5df122
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 04/17/2020
ms.locfileid: "81543744"
---
# <a name="server-controls"></a>サーバー コントロール

[マイクロソフト](https://github.com/microsoft)

> ASP.NET 2.0 では、サーバー コントロールがさまざまな点で強化されています。 このモジュールでは、2.0 と Visual Studio 2005 のサーバー コントロールに関するASP.NET方法に関するアーキテクチャの変更点について説明します。

ASP.NET 2.0 では、サーバー コントロールがさまざまな点で強化されています。 このモジュールでは、2.0 と Visual Studio 2005 のサーバー コントロールに関するASP.NET方法に関するアーキテクチャの変更点について説明します。

## <a name="view-state"></a>ビューステート

ASP.NET 2.0 でのビューステートの主な変更は、サイズの大幅な縮小です。 カレンダー コントロールのみを含むページを考えてみましょう。 ASP.NET 1.1 のビュー ステートを次に示します。

[!code-css[Main](server-controls/samples/sample1.css)]

ここでは、ASP.NET 2.0 の同じページのビュー ステートです。

[!code-css[Main](server-controls/samples/sample2.css)]

これは非常に大きな変更であり、ビューステートがネットワーク上で行き来することを考えると、この変更によって開発者は大幅なパフォーマンス向上を実現できます。 ビュー ステートのサイズの縮小は、内部での処理方法による結果が大きいです。 ビューステートは Base64 エンコード文字列であることを忘れないでください。 ASP.NET 2.0 のビュー ステートの変化をより深く理解するために、上記の例のデコードされた値を見てみましょう。

1.1 のビュー ステートがデコードされています。

[!code-css[Main](server-controls/samples/sample3.css)]

これは少しちんぷんかんぷんに見えるかもしれませんが、ここにはパターンがあります。 ASP.NET 1.x では、データ型を識別するために単一の文字を使用し、&lt;&gt;文字を使用して区切られた値を使用しました。 上記のビューステートサンプルの"t"は、トリプレットを表しています。 トリプレットには、配列リストのペアが含まれています ("l" は配列リストを表します)。これらの配列リストの 1 つに、値が 1 の Int32 ("i") が含まれ、もう 1 つの配列リストに別のトリプレットが含まれています。 トリプレットには、配列リストのペアなどが含まれています。覚えておくべきことは、ペアを含む三つ子を使用し、文字を使用してデータ型を識別し、および文字を&lt;&gt;区切り文字として使用することです。

ASP.NET 2.0 では、デコードされたビューステートは少し異なって見えます。

[!code-powershell[Main](server-controls/samples/sample4.ps1)]

デコードされたビュー ステートの外観に大きな変化があることに注意してください。 この変更には、いくつかのアーキテクチャの基盤があります。 ビュー ステート ASP.NET 1.x では、LosFormatter を使用してデータをシリアル化しました。 2.0 では、新しいクラスを使用します。 このクラスは、ビューステートとコントロールの状態のシリアル化と逆シリアル化を支援するために特別に設計されました。 (コントロールの状態については、次のセクションで説明します。シリアル化と逆シリアル化を行う方法を変更すると、多くの利点が得られます。 最も劇的なものの1つは、テキストライターを使用するLosFormatterとは異なり、ObjectStateFormatterがバイナリライターを使用しているという事実です。 これにより、ASP.NET 2.0 は、文字列ではなく一連のバイトをビューステートに格納できます。 たとえば、整数を考える。 ASP.NET 1.1 では、整数には 4 バイトのビューステートが必要でした。 ASP.NET 2.0 では、同じ整数は 1 バイトしか必要としな。 保存されているビュー ステートの量を減らすには、他の機能強化も行われました。 たとえば、DateTime の値は、文字列の代わりに TickCount を使用して格納されるようになりました。

そのすべてが十分ではなかったかのように、1.x でビューステートの最も偉大なコンシューマーの 1 つが DataGrid と同様のコントロールであるという事実に特別な注意が払われました。 ビューステートが関係する DataGrid などのコントロールの大きな欠点は、多くの場合、大量の繰り返される情報が含まれていることです。 ASP.NET 1.x では、その繰り返しの情報は、単純に何度も何度も格納され、ビューステートが肥大化しました。 ASP.NET 2.0 では、新しい IndexedString クラスを使用してこのようなデータを格納します。 文字列が繰り返される場合は、インデックス文字列のトークンとインデックスをインデックス付けされたオブジェクトの実行中のテーブルに格納するだけです。

## <a name="control-state"></a>コントロールの状態

開発者がビューステートに関して持っていた主な不満の 1 つは、HTTP ペイロードに追加されたサイズでした。 前述したように、ビューステートの最も大きいコンシューマーの 1 つは DataGrid コントロールです。 DataGrid によって生成される大量のビューステートを回避するために、多くの開発者は、そのコントロールのビューステートを無効にします。 残念ながら、その解決策は必ずしも良いものではありませんでした。 ASP.NET 1.x のビューステートには、コントロールの正しい機能に必要なデータだけでなく、含まれています。 また、コントロールの UI の状態に関する情報も含まれています。 つまり、DataGrid で改ページ調整を許可する場合は、ビューステートに含まれる UI 情報がすべて必要ない場合でも、ビューステートを有効にする必要があります。 これは、すべてまたは何もないシナリオです。

ASP.NET 2.0 では、制御状態の導入によって、制御状態がうまく解決されます。 コントロールの状態には、コントロールの適切な機能に必要なデータが含まれます。 ビューステートとは異なり、コントロールの状態を無効にすることはできません。 したがって、制御状態で格納されるデータを注意深く制御することが重要です。

> [!NOTE]
> コントロールの状態は、VIEWSTATE 非表示フォーム フィールド\_\_のビューステートと共に永続化されます。

このビデオは、ビューステートとコントロールの状態のチュートリアルです。

![](server-controls/_static/image1.png)

[フルスクリーンビデオを開く](server-controls/_static/state1.wmv)

サーバー コントロールがコントロールの状態を読み取りおよび書き込むには、3 つの手順を実行する必要があります。

## <a name="step-1-call-the-registerrequirescontrolstate-method"></a>ステップ 1: メソッドを呼び出します。

メソッドは、コントロールがコントロールの状態を永続化する必要があることをASP.NETに通知します。 これは、登録されているコントロールであるControl型の引数を1つ取ります。

登録は要求から要求までは存続しないことに注意してください。 したがって、コントロールがコントロールの状態を永続化する場合は、すべての要求でこのメソッドを呼び出す必要があります。 このメソッドは OnInit で呼び出されることをお勧めします。

[!code-csharp[Main](server-controls/samples/sample5.cs)]

## <a name="step-2-override-savecontrolstate"></a>ステップ 2: セーブコントロールステートをオーバーライドする

SaveControlStateメソッドは、コントロールのコントロールの状態の変更を最後のポスト バック以降に保存します。 コントロールの状態を表すオブジェクトを返します。

## <a name="step-3-override-loadcontrolstate"></a>ステップ 3: 読み込みコントロールの状態をオーバーライドする

メソッドは、保存された状態をコントロールに読み込みます。 このメソッドは、コントロールに保存された状態を保持する Object 型の引数を 1 つ受け取ります。

## <a name="full-xhtml-compliance"></a>完全な XHTML 準拠

Web 開発者は、Web アプリケーションにおける標準の重要性を知っています。 標準ベースの開発環境を維持するために、ASP.NET 2.0 は完全に XHTML に準拠しています。 したがって、すべてのタグは、HTML 4.0 以上をサポートするブラウザーで XHTML 標準に従ってレンダリングされます。

ASP.NET 1.1 の DOCTYPE 定義は次のとおりです。

[!code-html[Main](server-controls/samples/sample6.html)]

ASP.NET 2.0 では、デフォルトの DOCTYPE 定義は次のとおりです。

[!code-html[Main](server-controls/samples/sample7.html)]

選択した場合、構成ファイルの xhtmlConformance ノードを使用して、デフォルトの XHTML 準拠を変更できます。 たとえば、web.config ファイル内の次のノードは、XHTML のコンプライアンスを XHTML 1.0 Strict に変更します。

[!code-xml[Main](server-controls/samples/sample8.xml)]

選択した場合は、ASP.NET 1.x で使用される従来の構成を次のように使用するようにASP.NETを構成することもできます。

[!code-xml[Main](server-controls/samples/sample9.xml)]

## <a name="adaptive-rendering-using-adapters"></a>アダプタを使用したアダプティブ レンダリング

ASP.NET 1.x では、構成ファイルには&lt;、HttpBrowser 機能オブジェクトを設定する browserCaps&gt;セクションが含まれていました。 このオブジェクトを使用すると、開発者は、特定の要求を行っているデバイスを特定し、コードを適切にレンダリングできます。 ASP.NET 2.0 では、モデルが改善され、新しい ControlAdapter クラスが使用されるようになりました。 ControlAdapter クラスは、コントロールのライフサイクル内のイベントをオーバーライドし、ユーザー エージェントの機能に基づいてコントロールのレンダリングを制御します。 特定のユーザー エージェントの機能は、c:\windows\microsoft.net\framework\v2.0 に格納されているブラウザー定義ファイル (.browser ファイル拡張子を持つファイル) によって定義されます。\* \* \*\CONFIG\ブラウザ\*フォルダ。

> [!NOTE]
> クラスは抽象クラスです。

1.x&lt;の&gt;browserCaps セクションと同様に、ブラウザー定義ファイルでは、要求側のブラウザーを識別するために、正規表現を使用してユーザー エージェント文字列を解析します。 ユーザー エージェントは、そのユーザー エージェントの特定の機能を定義します。 コントロールアダプターは、レンダリング メソッドを使用してコントロールをレンダリングします。 したがって、Render メソッドをオーバーライドする場合は、基本クラスで Render を呼び出す必要はありません。 これを行うと、レンダリングが 2 回 (アダプターに 1 回、コントロール自体に 1 回) 発生する可能性があります。

## <a name="developing-a-custom-adapter"></a>カスタム アダプタの開発

ControlAdapter から継承して、独自のカスタム アダプターを開発できます。 また、ページにアダプタが必要な場合は、抽象クラス PageAdapter から継承できます。 カスタム アダプターへのコントロールのマッピングは、ブラウザー定義&lt;ファイルの&gt;controlAdapters 要素を使用して行われます。 たとえば、ブラウザー定義ファイルから次の XML は、Menu コントロールを MenuAdapter クラスにマップします。

[!code-html[Main](server-controls/samples/sample10.html)]

このモデルを使用すると、コントロール開発者が特定のデバイスやブラウザをターゲットにすることが非常に簡単になります。 また、開発者がすべてのデバイスでページをレンダリングする方法を完全に制御することも非常に簡単です。

## <a name="per-device-rendering"></a>デバイス単位のレンダリング

ASP.NET 2.0 のサーバー コントロール プロパティは、ブラウザ固有のプレフィックスを使用してデバイスごとに指定できます。 たとえば、次のコードでは、ページの閲覧に使用されているデバイスに応じてラベルのテキストが変更されます。

[!code-aspx[Main](server-controls/samples/sample11.aspx)]

このラベルを含むページを Internet Explorer から参照すると、ラベルに "Internet Explorer から参照中" というテキストが表示されます。 ページが Firefox から参照されると、ラベルに 「Firefox から閲覧中です」 というテキストが表示されます。 ページが他のデバイスから参照されると、「不明なデバイスから参照している」と表示されます。 この特殊な構文を使用して、任意のプロパティを指定できます。

## <a name="setting-focus"></a>フォーカスの設定

ASP.NET 1.x 開発者は、特定のコントロールに最初のフォーカスを設定する方法について頻繁に質問しました。 たとえば、ログイン ページでは、ページが最初に読み込まれたときに[ユーザー ID] テキスト ボックスにフォーカスを移動すると便利です。 ASP.NET 1.x では、これを行う場合は、クライアント側スクリプトを記述する必要があります。 このようなスクリプトは簡単な作業ですが、SetFocus メソッドのおかげで、2.0 ASP.NETで必要なくなりました。 SetFocus メソッドは、フォーカスを受け取るコントロールを示す引数を 1 つ取ります。 この引数には、コントロールのクライアント ID を文字列として指定するか、コントロールオブジェクトとしてサーバー コントロールの名前を指定できます。 たとえば、ページが最初に読み込まれたときに、最初のフォーカスを txtUserID という名前の TextBox コントロール\_に設定するには、次のコードをページ読み込みに追加します。

[!code-csharp[Main](server-controls/samples/sample12.cs)]

-- または

[!code-csharp[Main](server-controls/samples/sample13.cs)]

ASP.NET 2.0 では、Webresource.axd ハンドラ (前述) を使用して、フォーカスを設定するクライアント側の関数をレンダリングします。 クライアント側の関数の名前は、次に示\_すように Web フォームオートフォーカスです。

[!code-html[Main](server-controls/samples/sample14.html)]

または、コントロールに対して Focus メソッドを使用して、そのコントロールに最初のフォーカスを設定することもできます。 Focus メソッドは Control クラスから派生し、すべてのコントロールASP.NET使用できます。 検証エラーが発生したときに、特定のコントロールにフォーカスを設定することもできます。 これは後のモジュールで説明します。

## <a name="new-server-controls-in-aspnet-20"></a>ASP.NET 2.0 の新しいサーバー コントロール

ASP.NET 2.0 の新しいサーバー コントロールを次に示します。 後のモジュールで、それらについて詳しく説明します。

## <a name="imagemap-control"></a>イメージマップ コントロール

ImageMap コントロールを使用すると、ポストバックを開始したり、URL に移動したりできるホットスポットをイメージに追加できます。 利用できるホットスポットには3つのタイプがあります。サークルホットスポット、長方形ホットスポット、およびポリゴンホットスポット。 ホットスポットは、Visual Studio のコレクション エディターを使用するか、コードでプログラムによって追加されます。 イメージ上にホットスポットを描画するためのユーザー インターフェイスがありません。 ホットスポットの座標とサイズまたは半径は、宣言的に指定する必要があります。 また、デザイナーにはホットスポットの視覚的な表現もありません。 ホットスポットが URL に移動するように設定されている場合、その URL はホットスポットの NavigateUrl プロパティを使用して指定されます。 ポスト バック ホットスポットの場合、PostBackValue プロパティを使用すると、サーバー側のコードで取得できる文字列をポスト バックで渡すことができます。

![Visual Studio のホットスポット コレクション エディター](server-controls/_static/image1.jpg)

**図 1**: Visual Studio のホットスポット コレクション エディター

## <a name="bulletedlist-control"></a>箇条書きリスト コントロール

BulletedList コントロールは、データバインドが容易にできる箇条書きリストです。 リストは、BulletStyle プロパティを使用して順序付け (番号付き) または順序付けなしの場合があります。 リスト内の各項目は ListItem オブジェクトで表されます。

![コントロールの箇条書き](server-controls/_static/image1.gif)

**図 2**: Visual Studio の箇条書きリスト コントロール

## <a name="hiddenfield-control"></a>隠しフィールド コントロール

HiddenField コントロールは、ページに非表示フォーム フィールドを追加します。 隠しフォーム フィールドの値は、通常、ポスト バックの間に変更されないままにすることが期待されます。 ただし、悪意のあるユーザーがポストバックする前に値を変更することは可能です。 この場合、非表示フィールド コントロールは、値の変更イベントを発生させます。 隠しフィールド コントロールに機密情報があり、その変更が残っていることを確認する場合は、コードで ValueChanged イベントを処理する必要があります。

## <a name="fileupload-control"></a>ファイルのアップロード コントロール

ASP.NET 2.0 の FileUpload コントロールを使用すると、ASP.NET ページを使用して Web サーバーにファイルをアップロードできます。 このコントロールは、ASP.NET 1.x の HtmlInputFile クラスと非常によく似ていますが、いくつかの例外があります。 ASP.NET 1.x では、適切なファイルを持っているかどうかを判断するために、PostedFile プロパティの null をチェックすることをお勧めします。 ASP.NET 2.0 の FileUpload コントロールは、同じ目的で使用できる新しい HasFile プロパティを追加し、もう少し効率的です。

プロパティは HttpPostedFile オブジェクトにアクセスするために引き続き使用できますが、HttpPostedFile の機能の一部は FileUpload コントロールで本質的に使用できるようになりました。 たとえば、アップロードしたファイルを ASP.NET 1.x に保存するには、HttpPostedFile オブジェクトで SaveAs メソッドを呼び出します。 ASP.NET 2.0 の FileUpload コントロールを使用して、FileUpload コントロール自体で SaveAs メソッドを呼び出します。

2.0 の動作に大きな変更を与えた場合も(おそらく最も重要な変更)、アップロードしたファイル全体を保存する前にメモリにロードする必要がなくなったことが挙げられます。 1.x では、アップロードされたファイルは、ディスクに書き込まれる前にメモリに完全に保存されます。 このアーキテクチャでは、大きなファイルのアップロードを防止します。

ASP.NET 2.0 では、httpRuntime 要素の requestLengthDiskThreshold 属性を使用して、ディスクに書き込まれる前にメモリ内のバッファに保持されるキロバイト数を構成できます。

**重要**: MSDN のドキュメント (および他のドキュメント) では、この値が (KB ではなく) バイト単位であり、既定値は 256 であることを指定しています。 値は実際にはキロバイト単位で指定され、デフォルト値は 80 です。 デフォルト値が 80K の場合、バッファが大きなオブジェクト ヒープに置かれないようにします。

## <a name="wizard-control"></a>ウィザード コントロール

開発者がパネルを使用して、またはページ間で転送することによって、一連の「ページ」で情報を収集しようとするのに苦労しているASP.NETに遭遇することはかなり一般的です。 多くの場合、努力はイライラするものであり、時間がかかります。 新しい Wizard コントロールは、ユーザーが慣れ親しんだウィザード インターフェイスで線形および非線形のステップを可能にすることで、問題を解決します。 Wizard コントロールは、入力フォームを一連の手順で表示します。 各ステップは、コントロールの StepType プロパティで指定された特定の型です。 使用できるステップタイプは次のとおりです。

| **ステップタイプ** | **説明** |
| --- | --- |
| Auto | ウィザードは、ステップ階層内での位置に基づいてステップのタイプを自動的に決定します。 |
| [開始] | 最初のステップは、しばしば入門的な声明を提示するために使用される。 |
| 手順 | 通常のステップ。 |
| [完了] | ウィザードを完了するためのボタンを表示するために通常使用される最後の手順。 |
| 完了 | 成功または失敗を通知するメッセージを表示します。 |

> [!NOTE]
> Wizard コントロールは、コントロールの状態を使用して状態ASP.NET追跡します。 したがって、プロパティは、何の損害もせずに false に設定できます。

このビデオは、ウィザード コントロールのチュートリアルです。

![](server-controls/_static/image2.png)

[フルスクリーンビデオを開く](server-controls/_static/wizard1.wmv)

## <a name="localize-control"></a>コントロールのローカライズ

ローカライズ コントロールは、リテラル コントロールに似ています。 ただし、Localize コントロールには、追加するマークアップのレンダリング方法を制御する**Mode**プロパティがあります。 Mode プロパティは、次の値をサポートします。

| **モード** | **説明** |
| --- | --- |
| 変換 | マークアップは、要求を行うブラウザのプロトコルに従って変換されます。 |
| パススルー | マークアップは、その状態でレンダリングされます。 |
| エンコード | コントロールに追加されるマークアップは、HtmlEncode を使用してエンコードされます。 |

## <a name="multiview-and-view-controls"></a>マルチビューコントロールとビューコントロール

MultiView コントロールは、ビュー コントロールのコンテナーとして機能し、ビュー コントロールは、他のコントロールのコンテナー (パネル コントロールと同様) として機能します。 MultiView コントロール内の各ビューは、1 つのビュー コントロールで表されます。 MultiView の最初のビュー コントロールはビュー 0、2 番目のビューはビュー 1 などです。マルチビュー コントロールの ActiveViewIndex を指定してビューを切り替えることができます。

## <a name="substitution-control"></a>代替制御

置換コントロールは、ASP.NETキャッシュと組み合わせて使用されます。 キャッシュを利用する必要があるが、ページの各要求 (つまり、キャッシュから除外されるページの部分) で更新する必要がある部分がある場合、置換コンポーネントは優れたソリューションを提供します。 コントロールは、実際にはそれ自体で出力をレンダリングしません。 代わりに、サーバー側コードのメソッドにバインドされます。 ページが要求されると、メソッドが呼び出され、返されたマークアップが置換コントロールの代わりに表示されます。

代入コントロールがバインドされるメソッドは **、MethodName**プロパティを使用して指定します。 このメソッドは、次の条件を満たす必要があります。

- 静的 (VB で共有) メソッドである必要があります。
- 型の 1 つのパラメーターを受け取ります。
- ページ上のコントロールを置き換える必要があるマークアップを表す文字列を返します。

置換コントロールには、ページ上の他のコントロールを変更することはできませんが、そのパラメーターを使用して現在の HttpContext にアクセスできます。

## <a name="gridview-control"></a>グリッドビュー コントロール

コントロールは、DataGrid コントロールの代わりになります。 このコントロールについては、後のモジュールで詳しく説明します。

## <a name="detailsview-control"></a>詳細 ビュー コントロール

DetailsView コントロールを使用すると、データ ソースから 1 つのレコードを表示したり、データ ソースを編集または削除したりできます。 後のモジュールで詳しく説明します。

## <a name="formview-control"></a>フォーム ビュー コントロール

FormView コントロールは、構成可能なインターフェイスでデータ ソースから 1 つのレコードを表示するために使用します。 後のモジュールで詳しく説明します。

## <a name="accessdatasource-control"></a>コントロールのアクセスデータソース

コントロールは、Access データベースをデータ バインドするために使用されます。 後のモジュールで詳しく説明します。

## <a name="objectdatasource-control"></a>コントロールのオブジェクトデータ ソース

ObjectDataSource コントロールは、コントロールがデータ ソースに直接バインドされる 2 層モデルではなく、中間層ビジネス オブジェクトにコントロールをデータ バインドできるように、3 層アーキテクチャをサポートするために使用されます。 後のモジュールで詳しく説明します。

## <a name="xmldatasource-control"></a>コントロール

コントロールは、XML データ ソースへのデータ バインドに使用されます。 後のモジュールで詳しく説明します。

## <a name="sitemapdatasource-control"></a>コントロールを使用します。

コントロールは、サイト マップに基づいてサイト ナビゲーション コントロールのデータ バインディングを提供します。 後のモジュールで詳しく説明します。

## <a name="sitemappath-control"></a>サイトマップパスコントロール

SiteMapPath コントロールは、一般的に階層リンクと呼ばれる一連のナビゲーション リンクを表示します。 後のモジュールで詳しく説明します。

## <a name="menu-control"></a>メニュー コントロール

メニュー コントロールは、DHTML を使用して動的メニューを表示します。 後のモジュールで詳しく説明します。

## <a name="treeview-control"></a>TreeView コントロール

TreeView コントロールは、データの階層ツリー ビューを表示するために使用されます。 後のモジュールで詳しく説明します。

## <a name="login-control"></a>ログインコントロール

Login コントロールは、Web サイトにログインするためのメカニズムを提供します。 後のモジュールで詳しく説明します。

## <a name="loginview-control"></a>LoginView コントロール

LoginView コントロールでは、ユーザーのログイン状態に基づいて、さまざまなテンプレートを表示できます。 後のモジュールで詳しく説明します。

## <a name="passwordrecovery-control"></a>パスワード回復制御

PasswordRecovery コントロールは、ASP.NET アプリケーションのユーザーが忘れたパスワードを取得するために使用します。 後のモジュールで詳しく説明します。

## <a name="loginstatus"></a>LoginStatus

LoginStatus コントロールは、ユーザーのログイン状態を表示します。 後のモジュールで詳しく説明します。

## <a name="loginname"></a>LoginName

LoginName コントロールは、ASP.NETアプリケーションにログインした後、ユーザーのユーザー名を表示します。 後のモジュールで詳しく説明します。

## <a name="createuserwizard"></a>Createuserwizard

CreateUserWizard は、ASP.NET アプリケーションで使用するASP.NET メンバーシップ アカウントを作成する機能をユーザーに提供する構成可能なウィザードです。 後のモジュールで詳しく説明します。

## <a name="changepassword"></a>ChangePassword

ChangePassword コントロールを使用すると、ユーザーはASP.NET アプリケーションのパスワードを変更できます。 後のモジュールで詳しく説明します。

## <a name="various-webparts"></a>さまざまな Web パーツ

ASP.NET 2.0 にはさまざまな Web パーツが付属しています。 これらの詳細については、後のモジュールで説明します。
